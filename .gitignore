# generated each run
# seen.json is COMMITTED (dedup memory) — do NOT ignore it
# candidates.json is COMMITTED in the cloud-routine variant (cron pushes it);
# uncomment the next line only if your Claude task runs locally and reads the file directly
# candidates.json

# python
__pycache__/
*.pyc
.venv/
venv/

# os
.DS_Store
