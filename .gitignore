# generated each run
# seen.json is COMMITTED (dedup memory) — do NOT ignore it
# candidates.json is regenerated fresh every run and never committed — the
# routine's hard rule is it pushes seen.json only.
candidates.json

# python
__pycache__/
*.pyc
.venv/
venv/

# os
.DS_Store
